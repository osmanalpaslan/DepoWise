<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DepoWise.Desktop.ViewModels"
             xmlns:ctrl="using:DepoWise.Desktop.Controls"
             xmlns:lvc="using:LiveChartsCore.SkiaSharpView.Avalonia"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="1000" d:DesignHeight="680"
             x:Class="DepoWise.Desktop.Views.ReportsView"
             x:DataType="vm:ReportsViewModel">

    <Grid RowDefinitions="Auto,Auto,Auto,*,Auto" Background="{DynamicResource AppBackgroundBrush}" Margin="24">

        <ctrl:Toolbar Grid.Row="0" Title="Raporlar"/>

        <!-- Filtreler (ortak form bileşenleri) -->
        <Border Grid.Row="1" Classes="Panel" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal" Spacing="14" VerticalAlignment="Bottom">
                <ctrl:FormField Label="Rapor" Width="200">
                    <ComboBox Classes="Field" ItemsSource="{Binding ReportTypes}"
                              SelectedItem="{Binding SelectedReport}" HorizontalAlignment="Stretch"/>
                </ctrl:FormField>
                <ctrl:FormField Label="Başlangıç" Width="170">
                    <DatePicker Classes="Field" SelectedDate="{Binding FromDate}"/>
                </ctrl:FormField>
                <ctrl:FormField Label="Bitiş" Width="170">
                    <DatePicker Classes="Field" SelectedDate="{Binding ToDate}"/>
                </ctrl:FormField>
                <Button Classes="Primary" Content="Sorgula" Command="{Binding RunCommand}" VerticalAlignment="Bottom"/>
            </StackPanel>
        </Border>

        <!-- Grafik alanı: LiveCharts2 (gerçek rapor verisinden; sahte seri yok) -->
        <Border Grid.Row="2" Classes="Panel" Margin="0,16,0,0" Height="240">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="{Binding ChartTitle}" Classes="SectionTitle"
                           IsVisible="{Binding ShowChart}" Margin="0,0,0,8"/>
                <Panel>
                    <!-- Bar: araç bazında yakıt -->
                    <lvc:CartesianChart Series="{Binding ChartSeries}" XAxes="{Binding XAxes}" YAxes="{Binding YAxes}"
                                        IsVisible="{Binding ShowBar}" LegendPosition="Hidden" TooltipPosition="Top"/>
                    <!-- Pasta: stok durum dağılımı -->
                    <lvc:PieChart Series="{Binding PieSeries}" IsVisible="{Binding ShowPie}"
                                  LegendPosition="Hidden" TooltipPosition="Top"/>
                    <!-- Grafik yoksa/yüklenmediyse bilgi -->
                    <TextBlock Text="Grafik için Sorgula'ya basın." Classes="Helper"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               IsVisible="{Binding !ShowChart}"/>
                </Panel>
            </DockPanel>
        </Border>

        <!-- Tablo / durumlar -->
        <Panel Grid.Row="3" Margin="0,16,0,0">
            <Border Classes="Table" IsVisible="{Binding HasRows}">
                <DockPanel>
                    <Border Classes="TableHeader" DockPanel.Dock="Top">
                        <ItemsControl ItemsSource="{Binding Headers}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="x:String">
                                    <TextBlock Text="{Binding}" MinWidth="160"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                    <ListBox Classes="Table" ItemsSource="{Binding Rows}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <ItemsControl ItemsSource="{Binding}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="x:String">
                                            <TextBlock Text="{Binding}" MinWidth="160" VerticalAlignment="Center"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <ctrl:StatePanel Mode="Empty" Title="Rapor hazır" Message="Filtreleri seçip Sorgula'ya basın."
                             IsVisible="{Binding IsPrompt}"/>
            <ctrl:StatePanel Mode="Empty" Title="Sonuç yok" Message="Seçili kriterlere uygun veri bulunamadı."
                             IsVisible="{Binding IsEmptyResult}"/>
            <ctrl:StatePanel Mode="Error" Title="Rapor çalıştırılamadı" Message="{Binding LoadError}"
                             IsVisible="{Binding HasError}"/>
        </Panel>

        <TextBlock Grid.Row="4" Text="{Binding Status}" Margin="0,10,0,0"
                   Classes="Helper" Foreground="{DynamicResource TextSecondaryBrush}"/>
    </Grid>
</UserControl>

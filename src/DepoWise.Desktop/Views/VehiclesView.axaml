<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DepoWise.Desktop.ViewModels"
             xmlns:ctrl="using:DepoWise.Desktop.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="980" d:DesignHeight="620"
             x:Class="DepoWise.Desktop.Views.VehiclesView"
             x:DataType="vm:VehiclesViewModel">

    <Grid RowDefinitions="Auto,Auto,*,Auto" Background="{DynamicResource AppBackgroundBrush}" Margin="24">

        <ctrl:Toolbar Grid.Row="0" Title="Araçlar"
                      SearchText="{Binding Search, Mode=TwoWay}"
                      SearchWatermark="İç kod / plaka ara..."
                      PrimaryActionText="{Binding AddButtonText}"
                      PrimaryActionCommand="{Binding ToggleAddCommand}">
            <ctrl:Toolbar.FilterContent>
                <Button Classes="Secondary" Content="Ara" Command="{Binding LoadCommand}"/>
            </ctrl:Toolbar.FilterContent>
        </ctrl:Toolbar>

        <!-- Yeni Araç formu -->
        <Border Grid.Row="1" Classes="Panel" Margin="0,16,0,0" IsVisible="{Binding ShowAdd}">
            <Border.KeyBindings>
                <KeyBinding Gesture="Enter" Command="{Binding AddCommand}"/>
                <KeyBinding Gesture="Escape" Command="{Binding ClearCommand}"/>
            </Border.KeyBindings>
            <StackPanel Spacing="16" MaxWidth="720" HorizontalAlignment="Left">
                <TextBlock Text="YENİ ARAÇ" Classes="SectionTitle"/>

                <TextBlock Text="Tanım" Classes="CardLabel"/>
                <StackPanel Orientation="Horizontal" Spacing="14">
                    <ctrl:FormField Label="İç Kod" IsRequired="True" Width="180"
                                    HasError="{Binding HasCodeError}" ErrorText="{Binding CodeError}"
                                    HelpText="Benzersiz araç kodu.">
                        <TextBox Classes="Field" Text="{Binding NewCode}" TabIndex="0"/>
                    </ctrl:FormField>
                    <ctrl:FormField Label="Plaka" Width="180">
                        <TextBox Classes="Field" Text="{Binding NewPlate}" TabIndex="1"/>
                    </ctrl:FormField>
                    <ctrl:FormField Label="Üretim Yılı" Width="140">
                        <NumericUpDown Classes="Field" Value="{Binding NewYear}" Minimum="0" Maximum="2100"
                                       FormatString="0" ShowButtonSpinner="False" TabIndex="2"/>
                    </ctrl:FormField>
                </StackPanel>

                <TextBlock Text="Durum &amp; Sayaç" Classes="CardLabel"/>
                <StackPanel Orientation="Horizontal" Spacing="14">
                    <ctrl:FormField Label="Durum" Width="160">
                        <ComboBox Classes="Field" ItemsSource="{Binding StatusOptions}"
                                  SelectedItem="{Binding NewStatus}" TabIndex="3" HorizontalAlignment="Stretch"/>
                    </ctrl:FormField>
                    <ctrl:FormField Label="Sayaç" Width="160">
                        <NumericUpDown Classes="Field" Value="{Binding NewMeter}" Minimum="0"
                                       FormatString="0.##" ShowButtonSpinner="False" TabIndex="4"/>
                    </ctrl:FormField>
                    <ctrl:FormField Label="Birim" Width="120">
                        <ComboBox Classes="Field" ItemsSource="{Binding MeterUnits}"
                                  SelectedItem="{Binding NewMeterUnit}" TabIndex="5" HorizontalAlignment="Stretch"/>
                    </ctrl:FormField>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right">
                    <Button Classes="Secondary" Content="İptal" Command="{Binding ClearCommand}" TabIndex="7"/>
                    <Button Classes="Primary" Content="Kaydet" Command="{Binding AddCommand}" TabIndex="6"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Liste + Detay -->
        <Grid Grid.Row="2" Margin="0,16,0,0" ColumnDefinitions="*,360">
          <Panel Grid.Column="0">
            <Border Classes="Table" Grid.IsSharedSizeScope="True" IsVisible="{Binding HasRows}">
                <DockPanel>
                    <Border Classes="TableHeader" DockPanel.Dock="Top">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="c1"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="c3"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="c4"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="c5"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="c6"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="İÇ KOD" MinWidth="100"/>
                            <TextBlock Grid.Column="1" Text="PLAKA" MinWidth="120"/>
                            <TextBlock Grid.Column="2" Text="YIL" MinWidth="60"/>
                            <TextBlock Grid.Column="3" Text="SAYAÇ" MinWidth="110"/>
                            <TextBlock Grid.Column="4" Text="DURUM" MinWidth="100"/>
                            <TextBlock Grid.Column="5" Text="BAKIM/MUAYENE" MinWidth="120"/>
                        </Grid>
                    </Border>
                    <ListBox Classes="Table" ItemsSource="{Binding Items}" SelectedItem="{Binding Selected}">
                        <ListBox.ItemTemplate>
                            <DataTemplate x:DataType="vm:VehicleRow">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="c1"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="c3"/>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="c4"/>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="c5"/>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="c6"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Code}" MinWidth="100" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding PlateDisplay}" MinWidth="120" VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding PlateDisplay}"/>
                                    <TextBlock Grid.Column="2" Text="{Binding YearDisplay}" MinWidth="60" VerticalAlignment="Center"
                                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <TextBlock Grid.Column="3" Text="{Binding MeterDisplay}" MinWidth="110" VerticalAlignment="Center"/>
                                    <ctrl:StatusBadge Grid.Column="4" MinWidth="100" VerticalAlignment="Center"
                                                      Kind="{Binding StatusKind}" Text="{Binding StatusText}"/>
                                    <ctrl:StatusBadge Grid.Column="5" MinWidth="120" VerticalAlignment="Center"
                                                      Kind="{Binding AlertKind}" Text="{Binding AlertText}"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <ctrl:StatePanel Mode="Error" Title="Liste yüklenemedi" Message="{Binding LoadError}"
                             ActionText="Tekrar Dene" ActionCommand="{Binding LoadCommand}"
                             IsVisible="{Binding HasError}"/>
            <ctrl:StatePanel Mode="Empty" Title="Araç yok"
                             Message="Arama kriterine uygun araç bulunamadı."
                             IsVisible="{Binding IsEmpty}"/>
          </Panel>

          <!-- Detay / Düzenle / Sil -->
          <Border Grid.Column="1" Classes="Panel" Margin="16,0,0,0" IsVisible="{Binding HasSelection}">
            <ScrollViewer>
              <StackPanel Spacing="12">
                <Grid ColumnDefinitions="*,Auto">
                  <TextBlock Grid.Column="0" Text="{Binding Selected.Code}" Classes="PageTitle" VerticalAlignment="Center"/>
                  <ctrl:StatusBadge Grid.Column="1" Kind="{Binding Selected.StatusKind}" Text="{Binding Selected.StatusText}"/>
                </Grid>

                <ctrl:FormField Label="Plaka">
                  <TextBox Classes="Field" Text="{Binding EditPlate}" IsEnabled="{Binding IsEditing}"/>
                </ctrl:FormField>
                <ctrl:FormField Label="Üretim Yılı">
                  <NumericUpDown Classes="Field" Value="{Binding EditYear}" Minimum="0" Maximum="2100"
                                 FormatString="0" ShowButtonSpinner="False" IsEnabled="{Binding IsEditing}"/>
                </ctrl:FormField>
                <ctrl:FormField Label="Durum">
                  <ComboBox Classes="Field" ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding EditStatus}"
                            IsEnabled="{Binding IsEditing}" HorizontalAlignment="Stretch"/>
                </ctrl:FormField>
                <ctrl:FormField Label="Durum Açıklaması" IsVisible="{Binding IsMaintenanceStatus}"
                                HelpText="Yalnız 'Bakımda' durumunda saklanır.">
                  <TextBox Classes="Field" Text="{Binding EditStatusNote}" AcceptsReturn="True"
                           Height="60" IsEnabled="{Binding IsEditing}"/>
                </ctrl:FormField>
                <ctrl:FormField Label="Sayaç" HelpText="Yalnız ileri alınabilir (geriye gitmez).">
                  <NumericUpDown Classes="Field" Value="{Binding EditMeter}" Minimum="0"
                                 FormatString="0.##" ShowButtonSpinner="False" IsEnabled="{Binding IsEditing}"/>
                </ctrl:FormField>

                <WrapPanel>
                  <Button Classes="Primary" Content="Düzenle" Margin="0,0,8,8"
                          Command="{Binding BeginEditCommand}" IsVisible="{Binding !IsEditing}"/>
                  <Button Classes="Primary" Content="Kaydet" Margin="0,0,8,8"
                          Command="{Binding SaveEditCommand}" IsVisible="{Binding IsEditing}"/>
                  <Button Classes="Secondary" Content="İptal" Margin="0,0,8,8"
                          Command="{Binding CancelEditCommand}" IsVisible="{Binding IsEditing}"/>
                  <Button Classes="Danger" Content="Sil" Margin="0,0,8,8"
                          Command="{Binding RequestDeleteCommand}" IsVisible="{Binding !IsEditing}"/>
                </WrapPanel>

                <!-- Silme onayı -->
                <Border Classes="Panel" BorderBrush="{DynamicResource DangerBrush}" IsVisible="{Binding ConfirmDelete}">
                  <StackPanel Spacing="10">
                    <TextBlock Text="Bu aracı silmek istiyor musunuz?" Classes="Body" FontWeight="SemiBold"/>
                    <TextBlock Text="Kayıt çöp kutusuna alınır; geçmiş korunur." Classes="Helper" TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right">
                      <Button Classes="Secondary" Content="Vazgeç" Command="{Binding CancelDeleteCommand}"/>
                      <Button Classes="Danger" Content="Evet, Sil" Command="{Binding ConfirmDeleteVehicleCommand}"/>
                    </StackPanel>
                  </StackPanel>
                </Border>
              </StackPanel>
            </ScrollViewer>
          </Border>

          <!-- Seçim yokken ipucu -->
          <Border Grid.Column="1" Classes="Panel" Margin="16,0,0,0" IsVisible="{Binding !HasSelection}">
            <TextBlock Text="Detay/düzenleme için bir araç seçin." Classes="Helper"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>
        </Grid>

        <TextBlock Grid.Row="3" Text="{Binding Status}" Margin="0,10,0,0"
                   Classes="Helper" Foreground="{DynamicResource TextSecondaryBrush}"/>
    </Grid>
</UserControl>

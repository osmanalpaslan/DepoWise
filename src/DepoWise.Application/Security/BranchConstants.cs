namespace DepoWise.Application.Security;

/// <summary>Şube ile ilgili paylaşılan sabitler (web + masaüstü + API ortak).</summary>
public static class BranchConstants
{
    /// <summary>Login'de "Tüm Şubeler" seçimini temsil eden sanal şube kimliği (gerçek bir şube değildir).</summary>
    public const string AllBranchesId = "__all__";
}

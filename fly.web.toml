app = "depowise-web"
primary_region = "fra"

[build]
  dockerfile = "Dockerfile.web"

[env]
  Api__BaseUrl = "https://depowise-erp.fly.dev"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1
